

import java.util.Scanner;
import static org.junit.jupiter.api.Assertions.*;


public class Average {

    public double calcAvg(double num1, double num2, double num3) {

        return (num1 + num2 + num3) / 3;
        /* double sum = 0;
        int nextNum = 0;
        int n = 0;
        double average = 0;
        // create object to get input from user.
        Scanner scanner = new Scanner(System.in);

        // get N from user
        System.out.print("Enter amount of numbers to calculate average: ");
        n = scanner.nextInt();

        // for loop to get user input of numbers to average.
        for(int i = 0; i < n; i++) {
            System.out.print("Enter next number: ");
            nextNum = scanner.nextInt();
            sum += nextNum;
        }
        // Calculate average dividing sum by N

        return average = sum / N;




    */


    }
}












